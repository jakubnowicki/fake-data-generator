default:
  sass:
    input: "styles/main.scss"
    style: "compressed"
    output: "www/css/sass.min.css"
  help_values:
    integer:
      key: integer
      text: integer
    numeric:
      key: numeric
      text: numeric
    boolean:
      key: boolean
      text: boolean
    string:
      key: string
      text: string
    set:
      key: set
      text: set
    distribution:
      key: distribution
      text: distribution
    id:
      key: id
      text: id
    custom:
      key: custom
      text: custom
    custom_column:
      key: custom_column
      text: custom_column
    calculated:
      key: calculated
      text: calculated
